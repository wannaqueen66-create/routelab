
### 2026-03-03 补记：analysis-2/task1b（B1–B3 两阶段差异）已实现

- Lucas 追加需求：B1–B3 也要做 Round2-Round1 差异；预期总共 3 张图（B1/B2/B3）。
- 已实现并接入 pipeline：
  - 新脚本：`scripts/analysis2_b_stage_gap.py`
  - `scripts/pipeline.py` research 阶段自动运行。
  - 文档更新：`README.md`、`README_zh.md`、`RESULTS_MAP.md` 新增索引 Q12。
- 输出（results/research）：
  - `analysis2_b_stage_gap_long.csv`
  - `analysis2_b_stage_gap_figures/B1.png`, `B2.png`, `B3.png`
- 统计口径：C1-only（Complexity==1）硬约束；若发现 C0 出现 B 值直接报错；每个 group 内对 B1–B3 做 Holm 校正（`p_holm`）；p 为 Wilcoxon signed-rank；附 sr/dz。
- Git 提交：`89bf497`（push main）。
- Policy: each Telegram group is treated as a separate "employee" (separate session context). Global standards should be broadcast to all allowlisted groups.
- GitHub docs standard: for every GitHub project maintain (1) detailed bilingual README (Chinese+English) and (2) a separate Chinese-only markdown doc; README must be beginner-friendly, step-by-step, clear architecture, and include Table of Contents/目录.
- Telegram behavior: allowlisted groups reply without @mention; non-allowlisted groups should reply when Lucas @mentions (mention-gated). Also use 👀 as read/ack reaction when messages are seen.
- Preference: keep chat in natural language; do not surface tool-level error logs; summarize issues plainly and offer options.
- Runtime: Sam runs on a cloud VPS.
- GitHub access on VPS: SSH key configured; use SSH remotes for push/pull. Key: ~/.ssh/id_ed25519_github_lucas. Git identity: user.name=Lucas, user.email=lucas3451@qq.com.
- OpenClaw 2026.2.24+: google-antigravity-auth plugin is removed/unavailable; stale config entry was cleaned.
- EEG repo (wannaqueen66-create/eeg): prior push failures were due to local behind remote; resolved via fast-forward pull. Implemented Task7 scorecard output (task7_audit_scorecard.png + summary/overview) and pushed to main (commit db14608).
- Current allowlisted/employee groups (as of recent): notifications -5252356109; EEG -5207208617; SPSS/questionnaire -5296830759; eye-tracking -5247207387; cdac2025 -5167051718; chatgpt-team-helper -5184169529; routelab -5177204159. Removed deleted group -5190393732 and stale EEG id 5215759858.
- Lucas asked about automating campus network login (portal/802.1X) via router; pending details (auth type, router OS, captcha).
- Campus network details: portal URL https://s.scut.edu.cn/a79.htm?wlanacname=wushan (SCUT). Issue: when router runs OpenClash, client cannot open portal URL over WiFi; likely needs DIRECT rules / bypass fake-ip or disable OpenClash during login.
- Router environment: OpenWrt with OpenClash in Fake-IP mode; SCUT portal access requires bypass (Fake-IP filter + DIRECT rules for s.scut.edu.cn / scut.edu.cn).
- OpenClash portal bypass config: user added Fake-IP filter '+.s.scut.edu.cn' and custom rule 'DOMAIN-SUFFIX,s.scut.edu.cn,DIRECT'; recommended to broaden to '+.scut.edu.cn' and add 'DOMAIN,s.scut.edu.cn,DIRECT' + 'DOMAIN-SUFFIX,scut.edu.cn,DIRECT'.
- SCUT portal behavior question: user wants portal URL to be reachable even when not yet authenticated; bypass rules aim to keep portal domain direct/real-IP so login page remains accessible pre-auth (subject to campus walled-garden behavior).
- Network topology: router connects to campus Wi-Fi (portal auth) and provides Wi-Fi to Lucas devices; portal login/automation should occur on router upstream interface.
- Router interface details from Lucas: upstream dev apclix0 with IP 10.192.37.67, gateway 10.192.39.254; LAN br-lan 192.168.6.1/24. Next step: capture unauth redirect headers via curl -I to derive portal login endpoint.
