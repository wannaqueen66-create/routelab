# 2026-03-08

- Lucas要求删除本机 VPS 上的 Perplexity API 配置，并停止以后使用 Perplexity 进行联网搜索，因为额度已用完。
- 已从 `/root/.openclaw/openclaw.json` 中移除 `PPLX_API_KEY`，并将 `tools.web.search.enabled` 设为 `false`，避免后续继续误用。
- Lucas 说明仍然需要联网搜索最新资讯；随后已恢复通用联网搜索开关（`tools.web.search.enabled=true`），但保持 Perplexity API 已删除、未恢复。
- Lucas 提供 Brave Search API key，并要求对所有员工开放；已将 Brave key 写入全局 `env.vars.BRAVE_API_KEY` 与 `tools.web.search.apiKey`，使全体群组员工可继承使用通用联网搜索。
