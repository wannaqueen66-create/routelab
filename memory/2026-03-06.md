# 2026-03-06

- Lucas confirmed EEG (eeg), questionnaire/SPSS (spss), and eye-tracking (eyetrack-aoi/eyetrack) projects are part of the same experiment; some memory should be shared across these groups.
- Created shared context file /root/.openclaw/workspace/EXPERIMENT_SHARED_CONTEXT.md and instructed the three groups to use it; updated their systemPrompts accordingly and announced in notifications group.
- Lucas feedback: when a task is completed, Sam must explicitly report back completion; avoid "read and no reply" so Lucas can confirm it was done.
- Added Telegram whitelist group -5283262605 and defined its role as yyd assistant; set requireMention=false with a dedicated system prompt.
- In eyetrack repo work, Lucas wants output organization optimized around practical analysis views: scene-based folders, participant-based folders, grouped summaries by SportFreq and Experience (explicitly no 2x2 cross grouping), and publication-style PNGs aligned with Building and Environment.
- Lucas strongly prefers final AOI optimized outputs to be inspectable from the merged/summary folder perspective; current confusion came from optimized outputs being generated under per-size batch folders instead of the merged folder.
- Grouped plot scene naming should follow group_manifest trial metadata columns such as trial01_scene / trial01_WWR / trial01_Cond, producing friendly labels like WWR45_C1 instead of raw scene_id.
- Lucas is using Colab mainly via direct code cells (not notebook examples), so repo changes should be easy to integrate by small edits to existing cmd lists rather than assuming standalone example scripts.
- Lucas is sensitive to Telegram-formatted pasted code causing indentation errors in Colab; when guiding edits, prefer minimal patch-style instructions (replace these exact lines / insert after this block) rather than reposting large full functions.
- Important bug learned on 2026-03-06 eyetrack workflow: pushing repo support alone was not enough because Lucas's outer Colab script handles size-split batching and merged-folder creation itself. To make merged optimized outputs appear under 输出结果_AOI_合并/optimized_outputs, guidance must patch the Colab merge script to run optimize_aoi_outputs.py after merged CSV creation.
- In EEG group work on 2026-03-06, Lucas confirmed the eye-tracking CSVs are already manually cut into per-subject × per-scene view segments; marker-based segmentation is only on the EEG/NIC2 side, not eye-tracking.
- EEG and eye-tracking subject identifiers are Chinese participant names, so multimodal merge should use Chinese names as subject_id plus scene_id/block_id/cycle_in_block.
- For EEG scene naming, design labels should continue following the shared long design table; do not invent a separate eye-specific scene naming scheme. scene_name/WWR/Cond/Complexity should stay aligned to the EEG design table.
- In eeg repo on 2026-03-06, added an eye-tracking integration pipeline: scene-level eye summary builder, eye feature merge into all_subjects_scene_level_with_eye.csv, rule-based eye QC flags/reports, and minimal EEG×eye exploratory analysis/reporting. Commits pushed: 777f2b3, ec28427, 516c2c1, 9c86b3d, 24a47a6, 758bded, 7a55903.
